/* Varnam Productions – Netflix card hover (no gate / no profiles) */
export const initNetflixExperience = () => {
  // Nothing needed – all hover effects are pure CSS.
  // This file is kept so the main.js import remains valid.
};
